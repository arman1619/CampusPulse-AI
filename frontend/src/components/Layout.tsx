import{NavLink,Outlet}from'react-router-dom';import{useAuth}from'../contexts/AuthContext';const nav={STUDENT:[['Overview','/student'],['Submit feedback','/student/submit'],['My feedback','/student/issues']],STAFF:[['Overview','/staff'],['Assigned issues','/staff/issues'],['Priority queue','/staff/priority']],ADMIN:[['Overview','/admin'],['All issues','/admin/issues'],['AI analytics','/admin/analytics'],['Users','/admin/users'],['Audit','/admin/audit']]} as const;export default function Layout(){const{user,logout}=useAuth();if(!user)return null;return <div className="shell"><aside className="sidebar"><div className="brand"><div className="brand-mark">CP</div><div><strong>CampusPulse AI</strong><small>Issue Management Platform</small></div></div><nav aria-label="Main navigation">{nav[user.role].map(([label,to])=><NavLink key={to} to={to} end={to==='/student'||to==='/staff'||to==='/admin'}>{label}</NavLink>)}<NavLink to="/assistant">AI Assistant</NavLink><NavLink to="/notifications">Notifications</NavLink><NavLink to="/profile">Profile</NavLink></nav><button className="secondary full" onClick={logout}>Sign out</button></aside><main className="main"><header className="topbar"><div><span className="eyebrow">{user.role}</span><strong>{user.full_name}</strong></div><span className="environment">Campus Operations</span></header><div className="content"><Outlet/></div></main></div>}