import{Link}from'react-router-dom';import type{Feedback}from'../types';import StatusBadge from'./StatusBadge';export default function IssueTable({items,empty='No issues found.'}:{items:Feedback[];empty?:string}){if(!items.length)return <div className="empty-state">{empty}</div>;return <div className="table-wrap"><table><thead><tr><th>Issue</th><th>Status</th><th>Category</th><th>Priority</th><th>AI review</th><th>Created</th></tr></thead><tbody>{items.map(i=><tr key={i.id}><td><Link to={`/issues/${i.id}`}><strong>{i.title}</strong></Link><small>{i.location||'Location not specified'}</small></td><td><StatusBadge value={i.status}/></td><td>{i.final_category||'Pending'}</td><td><StatusBadge value={i.final_priority}/></td><td>{i.ai_needs_review?'Required':'No'}</td><td>{new Date(i.created_at).toLocaleDateString()}</td></tr>)}</tbody></table></div>}