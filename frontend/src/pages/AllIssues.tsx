import{useEffect,useState}from'react';import api from'../api/client';import IssueTable from'../components/IssueTable';import type{Feedback}from'../types';export default function AllIssues(){const[items,setItems]=useState<Feedback[]>([]);const[q,setQ]=useState('');const[status,setStatus]=useState('');useEffect(()=>{const id=setTimeout(()=>api.get('/feedback',{params:{q:q||undefined,status:status||undefined}}).then(r=>setItems(r.data)),200);return()=>clearTimeout(id)},[q,status]);return <><div className="page-heading"><div><span className="eyebrow">Global issue register</span><h1>All issues</h1></div></div><div className="filters"><label>Search<input value={q} onChange={e=>setQ(e.target.value)} placeholder="Title, description or location"/></label><label>Status<select value={status} onChange={e=>setStatus(e.target.value)}><option value="">All statuses</option>{['SUBMITTED','ASSIGNED','IN_PROGRESS','RESOLVED','CLOSED','REOPENED'].map(x=><option key={x}>{x}</option>)}</select></label></div><section className="panel"><IssueTable items={items}/></section></>}