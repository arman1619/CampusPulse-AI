import{useEffect,useState}from'react';import api from'../api/client';type Row={id:string;action:string;actor_user_id?:string;resource_id:string;timestamp:string;metadata_json?:string};export default function AuditView(){const[rows,setRows]=useState<Row[]>([]);useEffect(()=>{Promise.all([api.get('/audit'),api.get('/auth/audit')]).then(([a,b])=>setRows([...a.data,...b.data].sort((x,y)=>+new Date(y.timestamp)-+new Date(x.timestamp))))},[]);return <><div className="page-heading"><div><span className="eyebrow">Traceability</span><h1>Audit view</h1><p>Security-sensitive and workflow-changing operations are retained as immutable application audit events.</p></div></div><section className="panel table-wrap"><table><thead><tr><th>Time</th><th>Action</th><th>Actor</th><th>Resource</th><th>Metadata</th></tr></thead><tbody>{rows.map(r=><tr key={r.id}><td>{new Date(r.timestamp).toLocaleString()}</td><td>{r.action}</td><td><code>{r.actor_user_id?.slice(0,8)||'system'}</code></td><td><code>{r.resource_id.slice(0,8)}</code></td><td><small>{r.metadata_json||'{}'}</small></td></tr>)}</tbody></table></section></>}