import{useState}from'react';import{Link,useNavigate}from'react-router-dom';import{useAuth}from'../contexts/AuthContext';export default function Login(){const{login}=useAuth();const nav=useNavigate();const[email,setEmail]=useState('student@campuspulse.dev');const[password,setPassword]=useState('Student123!');const[error,setError]=useState('');const[busy,setBusy]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);setError('');try{const u=await login(email,password);nav(u.role==='ADMIN'?'/admin':u.role==='STAFF'?'/staff':'/student')}catch(err:any){setError(err.response?.data?.detail||'Login failed')}finally{setBusy(false)}}return <div className="auth-page"><section className="auth-brand"><span className="eyebrow">SWE7303 • Cloud-native DevOps demonstration</span><h1>Campus feedback becomes actionable operational intelligence.</h1><p>Secure role-based issue management with hosted Llama triage, human oversight, auditability and deployment automation.</p></section><form className="auth-card" onSubmit={submit}><h2>Sign in</h2><p className="muted">Use your CampusPulse account.</p><label>Email<input type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></label><label>Password<input type="password" value={password} onChange={e=>setPassword(e.target.value)} required/></label>{error&&<div className="error" role="alert">{error}</div>}<button disabled={busy}>{busy?'Signing in…':'Sign in'}</button><p>New student? <Link to="/register">Create an account</Link></p></form></div>}