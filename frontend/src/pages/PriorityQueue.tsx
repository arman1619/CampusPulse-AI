import{useEffect,useState}from'react';import api from'../api/client';import IssueTable from'../components/IssueTable';import type{Feedback}from'../types';export default function PriorityQueue(){const[items,setItems]=useState<Feedback[]>([]);const[priority,setPriority]=useState('CRITICAL');useEffect(()=>{api.get('/feedback',{params:{priority}}).then(r=>setItems(r.data))},[priority]);return <><div className="page-heading"><div><span className="eyebrow">Risk-based triage</span><h1>Priority queue</h1></div><label className="compact">Priority<select value={priority} onChange={e=>setPriority(e.target.value)}>{['CRITICAL','HIGH','MEDIUM','LOW'].map(x=><option key={x}>{x}</option>)}</select></label></div><section className="panel"><IssueTable items={items}/></section></>}