import{useEffect,useState}from'react';import api from'../api/client';import IssueTable from'../components/IssueTable';import type{Feedback}from'../types';export default function MyFeedback(){const[items,setItems]=useState<Feedback[]>([]);useEffect(()=>{api.get('/feedback').then(r=>setItems(r.data))},[]);return <><div className="page-heading"><div><span className="eyebrow">Issue history</span><h1>My feedback</h1><p>All reports associated with your authenticated account.</p></div></div><section className="panel"><IssueTable items={items}/></section></>}