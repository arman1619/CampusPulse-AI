import{useEffect,useState}from'react';import api from'../api/client';import IssueTable from'../components/IssueTable';import type{Feedback}from'../types';export default function AssignedIssues(){const[items,setItems]=useState<Feedback[]>([]);const[status,setStatus]=useState('');useEffect(()=>{api.get('/feedback',{params:{assigned:'me',status:status||undefined}}).then(r=>setItems(r.data))},[status]);return <><div className="page-heading"><div><span className="eyebrow">Staff queue</span><h1>Assigned issues</h1></div><label className="compact">Status<select value={status} onChange={e=>setStatus(e.target.value)}><option value="">All</option>{['ASSIGNED','IN_PROGRESS','RESOLVED','CLOSED','REOPENED'].map(x=><option key={x}>{x}</option>)}</select></label></div><section className="panel"><IssueTable items={items}/></section></>}